2017/07/25

git version: ?

result:
+ continuous ok, comparison failed again
+ no data display


change:
+ iteration parameter "array" declared as np.array beforehands

result:
+ still a future warning about comparison


change:
+ np.array(dtype=int)

result:
+ nicht ok
+ counter joinen nicht, wenn sie nicht richtig abgebrochen werden

change:
+ join alle counter und dataprocesser
+ add [] to np.array

result:
+ join ok
+ comparison still broken

change:
+ dtype als c_uint32

result:
+ comparison failed

change:
+ array declared as object()
+ print(n3)

result:
+ comparison failed
+ printed strange numbers, negative and decreasing

change:
+ self._N zu n3 print,
+ rollover corrections dismissed

result:
+ comparison failed, but just a warning
+ counting correct -> 5MHz sampling -> result value 0.05 -> *10**8 -> 5MHz

change:
+ start laser control

result:
+ self._green.StartTask() -> resource is reserved

change:
+ counter: 0, 1, 3, 4
+ laser: 2, 5, 6, 7
+ join laser

result:
+ counter ok
+ laser ok
+ 1kHz and 10kHz perfect
+ continuous mode ok
+ finite mode: duration(int) not iterable

change:
+ make exit print and __del__() print for laser class also
+ make duration a numpy array, list are no iterables

result:
+ continuous ok
+ finite ok, after stopping it crashes

change:
+ queues ins init, vllt läuft bei der neubelegung was schief

result:
+ gleiches probelm, python crashes

change:
+ waitforiteration gets the power to start measurement processes
+ process start in own function, waitfor emits signal

result:
+ works fine
+ sometimes dataprocesser does not join

change:
+ increase timeout to join
+ decrease animation parameters, such as frame and interval

result:
+ ok behavior
+ seemingly all processes joined
+ animation has two different yscales

change:
+ change yscale
+ change plot to appear in one, install seaborn

result:
+ ok, but process doesnt join, maybe too much to calculate


